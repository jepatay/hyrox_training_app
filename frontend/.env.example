# The secret token that must be in the URL: /?access=YOUR_TOKEN
# Must match ACCESS_TOKEN in backend .env
VITE_ACCESS_TOKEN=your_secret_token_here

# Backend API URL (leave empty for same-origin / proxy in dev)
VITE_API_URL=
