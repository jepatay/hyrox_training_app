rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the backend using Firebase Admin SDK.
    // The frontend does NOT connect directly to Firestore.
    // Block all direct client access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
